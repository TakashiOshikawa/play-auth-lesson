var mainCtrl = function($scope, $http){

	// サインアップかログインの切り替え
	$scope.changeCard = function(){
		// $scope.tasks.push( {"body":$scope.newTaskBody, "doen":false, "num":$scope.tasks.length+1} );
		// $scope.newTaskBody = '';
		// $scope.count_num = $scope.tasks.length;
		// alert("clicked here");
	}

}